# Turbo Utilities
Utillity code shared by turbo_control_code and remote_control

Some libraries needed by one project won't be present in the other project, so not all code is usable. If you needed to use it, you would have to install the dependencies.